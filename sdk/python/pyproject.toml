[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "agentfileops"
version = "0.1.0"
description = "Python client for the verified AgentFileOps policy CLI"
requires-python = ">=3.9"
license = { text = "MIT" }

[tool.hatch.build.targets.wheel]
packages = ["agentfileops"]
